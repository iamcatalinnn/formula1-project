name: 'formula1_project'
version: '1.0.0'
config-version: 2

profile: 'f1_project_profile'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets: 
  - "target"
  - "dbt_packages"
  - "logs"

# --- CORE SCHEMA MAPPING CONFIGURATION ---
models:
  formula1_project:
    +schema: gold

    staging:
      +schema: bronze

    intermediate:
      +schema: silver
      
    marts:
      +schema: gold